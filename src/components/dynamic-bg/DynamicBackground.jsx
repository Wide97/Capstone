import React from "react";
import "./dynamic-background.scss";


/**
 * Componente di sfondo dinamico con effetto overlay, griglia animata e luce radiale.
 * Rimane fisso in background, sotto tutti gli altri elementi.
 */
const DynamicBackground = () => {
  return <div className="dynamic-background"></div>;
};

export default DynamicBackground;
